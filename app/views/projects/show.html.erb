<%= content_for :title, "Ruby on Rails code stats and metrics for #{@project.name}" %>

<h1><%= @project.name %></h1>

<ul>
  <% @project.current_snapshot.metrics.each do |metric| %>
    <li>
      <%= metric.name %>
      <%= metric.lines %>
    </li>
  <% end %>
</ul>
